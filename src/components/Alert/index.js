function Alert(props) {
    const { children } = props;

    return <div style={{ color: '#ea868f', fontSize: '13px', marginTop: '-10px', marginBottom: '10px' }}>{children}</div>;
}

export default Alert;
